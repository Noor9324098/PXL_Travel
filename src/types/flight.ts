export type Flight = {
  id: string;
  origin: string;
  destination: string;
  date: string;
  airline: string;
  duration: string;
  price: string;
  departureTime: string;
  arrivalTime: string;
};